@model CityViewModel

<div class="container-md"> 
<h1>Админ. панель > Города > Изменение города @Model.Title</h1>

<a asp-action="AdminPanel" asp-controller="DataWorker">Вернуться ко списку городов</a>
<br />

<form method="post" asp-action="EditCity" asp-controller="DataWorker">
    <p>
        <div class="col-md-3"
        <label>Идентификатор города</label><br />
        <input type="text" class="form-control" readonly asp-for="@Model.CityId" /><br />
        </div>
    </p>
    <p>
        <div class="col-md-3">
        <label >Название города</label> <br />
        <input type="text" class="form-control" asp-for="@Model.Title" />
        </div>
    </p>
    <p>
        <label >Поликлиники города</label>
        <div class="dropdown">
  <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
    Добавить поликлинику
  </button>
  <ul class="dropdown-menu">
      @foreach(var polyToAdd in Model.OtherPolyclinics)
      {
        <li><a class="dropdown-item" asp-action="AddPolyInCity" asp-controller="DataWorker" asp-route-cityID="@Model.CityId" asp-route-polyId="@polyToAdd.Id">@polyToAdd.Title</a></li>
      }
  </ul>
        </div>
        <br /> 

        <table class="table table-hover">
            @foreach(var poly in Model.PolyclinicsOfCity)
            {
                <tr>
                    <td>
                    @poly.Title
                    </td>
                    <td>
                        <a asp-controller="DataWorker" asp-action="RemovePolyInCity" asp-route-polyId="@poly.Id" asp-route-cityId="@Model.CityId">Удалить</a>
                    </td>
                </tr>
            }
        </table>
    </p>
    <p>
        <input type="submit" class="btn btn-primary" value="Сохранить" />
    </p>
</form>
</di>