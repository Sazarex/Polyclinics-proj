@using System.Security.Claims
<!DOCTYPE html>

<html>
<head>
    @await Html.PartialAsync("HeadPartial")
    


</head>
<body style="background-color: #f8f8ff">
    
    <div >
        

            <nav class="navbar navbar-expand-lg bg-light" >
      <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarNav" style="justify-content:space-between" >
          <ul class="navbar-nav">
            <li class="nav-item">
                @if(@User.Identity.IsAuthenticated)
                    {
                        <a class="nav-link" asp-action="LogOff" asp-controller="Auth">Выйти из системы</a>
                    }
                    else
                    {
                        <a class="nav-link" asp-action="Login" asp-controller="Auth">Авторизоваться</a>
                    }
                </li>
                @if(@User.Identity.Name == "Admin")
                   {
                       <li class="nav-item">
                    <a class="nav-link" asp-area="Admins" asp-action="AdminPanel" asp-controller="DataWorker">Админ панель</a>
                        </li>
                   }
            <li class="nav-item">
                <a class="nav-link" asp-area="Users" asp-action="Index" asp-controller="Home">Главная</a>
            </li>
          </ul>
          
          @if(User.Identity.IsAuthenticated)
          {
              <div >
          <span class="navbar-text" >
                @User.Identity.Name
                </span>
                        </div>
                    }
                    else
                    {
                      <div >
                  <span class="navbar-text" >
                        Не авторизован
                        </span>
                        </div>
                    }
                </div>
          </div>

          
        
    </nav>











    @*<div>
        @if(@User.Identity.IsAuthenticated)
        {
        <a asp-action="LogOff" asp-controller="Auth">Выйти из системы</a>
        }
        else
        {
        <a asp-action="Login" asp-controller="Auth">Авторизоваться</a>
        }

        @if(@User.Identity.Name == "Admin")
       {
        <a asp-action="AdminPanel" asp-controller="Auth">Админ панель</a>
       }

        <a asp-action="Index" asp-controller="Home">Главная</a>
        @await Html.PartialAsync("_UserInfo")
    </div>*@





        @RenderBody()
    </div>
</body>
</html>
